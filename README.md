# 📝 To-Do List Web Application  
### (Django + Raw SQL + REST API + Templates + Docker)

This project is a complete To-Do List management system built using **Django** without using Django ORM.  
All database operations use **Raw SQL** to meet system requirements.

---

## ✨ Features

- Full **CRUD** functionality for tasks
- **RESTful JSON APIs** for integration
- Responsive **HTML UI** using Django Templates + Bootstrap
- **SQLite** database using Raw SQL queries
- Dockerized — ready for deployment anywhere
- Logging & basic error handling
- Automated tests using **pytest**

---

## 🧱 Tech Stack

| Area | Technology |
|------|------------|
| Backend | Django (Python) |
| Database | SQLite (Raw SQL only — No ORM) |
| API | Django REST-style views (manual JSON responses) |
| Frontend | Django Templates, Bootstrap |
| Testing | Pytest + pytest-django |
| Deployment | Docker |
| Version Control | Git |

---

## 📁 Project Structure

---

## ▶ Setup Instructions

### 1️⃣ Clone the Repository
```bash
git clone https://github.com/RakhiAg158/Python_PELOCAL_ProjectWithAPIsAndTemplates
cd Python_PELOCAL_ProjectWithAPIsAndTemplates/backend
